#include<stdio.h>
struct Date
{
  int month;
  int day;
  int year;
};
struct date ScanDate(){
	struct today;
    gets(today.month); fflush(stdin);
    gets(today.day); fflush(stdin);
    gets(today.year); fflush(stdin);
    printf("Enter a date (mm dd yyyy):");
}
